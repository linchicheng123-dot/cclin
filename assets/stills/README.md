# Still image assets

Place final AVIF or WebP artwork in this directory, then add each file to
`src/content.ts` under the matching `stillWorks` entry.

Keep unfinished entries as `draft`. Change the status to `published` only
when the artwork, bilingual copy, and alt text are ready.
